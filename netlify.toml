# Netlify build settings for Vite + React
[build]
  command = "npm run build"
  publish = "dist"

# SPA redirects (important for React routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
